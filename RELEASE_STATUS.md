# Release Status — v2.0.0

**Target:** Core V2 / DNS leak-resistant baseline

```text
main @ 2.0.0
  → Validate / Audit / Test / Golden / Check
  → Build seven-platform artifacts
  → Tag v2.0.0 (tag-only release)
  → latest-rules + Manifest + SHA256
```

Immutable release gate unchanged: failed pipeline must not update `latest-rules`.
