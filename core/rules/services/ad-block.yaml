# Ad block rules / 广告拦截规则
version: 1
id: service-ad-block
group: ad-block
priority: 1

rules:
  - type: geosite
    values: [category-ads-all]
    action: group
    target: ad-block
  # More specific ad lists can be added later via rule-providers
