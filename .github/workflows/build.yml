name: Build Platform Configs

on:
  workflow_dispatch:
  push:
    paths:
      - 'core/**'
      - 'platforms/**'
      - 'scripts/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run build script
        run: |
          echo "Build placeholder - to be implemented"
